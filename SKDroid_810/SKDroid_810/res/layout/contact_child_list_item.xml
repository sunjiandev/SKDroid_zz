<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/contact_child_itemlayout"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/list_sel_green"
    android:clickable="false"
    android:descendantFocusability="blocksDescendants"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="4dip"
    android:paddingLeft="8dip"
    android:paddingRight="12dip" >

    <LinearLayout
        android:id="@+id/icon"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/login_head_bg"
        android:orientation="vertical"
        android:padding="1dp" >

        <ImageView
            android:id="@+id/contact_child_item_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:focusable="false"
            android:padding="0dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_launcher" />
    </LinearLayout>

    <RelativeLayout
        android:id="@+id/contact_right_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:gravity="center" >

        <TextView
            android:id="@+id/contact_child_item_name"
            android:layout_width="60dp"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:ellipsize="marquee"
            android:focusable="false"
            android:focusableInTouchMode="true"
            android:gravity="center"
            android:marqueeRepeatLimit="marquee_forever"
            android:padding="2dp"
            android:singleLine="true"
            android:text="19811208005"
            android:textColor="@color/color_titleblack"
            android:textSize="14dp" />

        <TextView
            android:id="@+id/contact_child_item_isonline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/contact_child_item_name"
            android:layout_centerHorizontal="true"
            android:focusable="false"
            android:padding="2dp"
            android:singleLine="true"
            android:text="@string/online"
            android:textColor="@color/color_titleblack"
            android:textSize="12.0sp"
            android:visibility="visible" />
    </RelativeLayout>

</LinearLayout>